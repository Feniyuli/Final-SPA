spring:
  datasource:
    url: jdbc:h2:mem:dinner-dev-db;LOCK_TIMEOUT=10000;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driverClassName: org.h2.Driver
    username: sa
    password: ''
    schema-generate: CREATE
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create